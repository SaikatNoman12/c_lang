#include <stdio.h>

int main(){

    int a, b, sum;

    a = 103;
    b = 103;

    sum = a + b;

    printf("%d + %d = %d. ", a, b, sum);

    // use float number;
    int c = 50.32, d = 50, sum2;
    sum2 = c + d;
    printf("Sum2 is %d.", sum2);

}